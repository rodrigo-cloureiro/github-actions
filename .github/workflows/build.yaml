name: Build Python

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"
      
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: List
        run: ls -ll
